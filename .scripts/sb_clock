#!/bin/sh

# clock - outputs the current date and time in "DAY, MM DD YYYY | HH:MM:SS" format.

DATE=$(date "+%a, %b %d %Y")
TIME=$(date "+%T")

printf "📆 %s 🕘 %s\n" "$DATE" "$TIME"

### END OF FILE
